import {ExcelController} from "./ExcelController";
import {Range, WorkSheet} from "xlsx";

export function copyRange(this: ExcelController, sourceRange: Range, targetRange: Range, target: object) {

}